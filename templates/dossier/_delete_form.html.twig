<form method="post" action="{{ path('dossier_delete', {'id': dossier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier.id) }}">
    <button class="btn">Delete</button>
</form>
