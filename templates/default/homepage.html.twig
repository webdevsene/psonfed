{# {% extends 'base.html.twig' %} #}
{% extends "/psonfedhead/layout_menu.html.twig" %}

{# {% block body_id 'homepage' %} #}

{#
    the homepage is a special page which displays neither a header nor a footer.
    this is done with the 'trick' of defining empty Twig blocks without any content
#}
{# {% block header %}{% endblock %}
   {% block footer %}{% endblock %} #}

   
{% block body_id 'login' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('login') }}
{% endblock %}

{% block body %}
{#     <div class="page-header">
        <h1>{{ 'title.homepage'|trans|raw }}</h1>
       </div>

    <div class="row">
        <div class="col-sm-6">
            <div class="jumbotron">
                <p>
                    {{ 'help.browse_app'|trans|raw }}
                </p>
                <p>
                    <a class="btn btn-primary btn-lg" href="{{ path('blog_index') }}">
                        <i class="fa fa-users" aria-hidden="true"></i> {{ 'action.browse_app'|trans }}
                    </a>
                </p>
            </div>
        </div>

        <div class="col-sm-6">
            <div class="jumbotron">
                <p>
                    {{ 'help.browse_admin'|trans|raw }}
                </p>
                <p>
                    <a class="btn btn-primary btn-lg" href="{{ path('admin_index') }}">
                        <i class="fa fa-lock" aria-hidden="true"></i> {{ 'action.browse_admin'|trans }}
                    </a>
                </p>
            </div>
        </div>
    </div> #}


    
            
            <div class="hero" style="background-image: url('../bootstrap/images/hero_2.jpg');">
                
                <div class="row">
                    <div class="col-md-8 offset-md-2" style="margin-left: 38%; margin-top: 17%;">
                        {% if app.user %}
                            {# do anythink if there are not no user login in #}
                        {% else %}
                            {# do this if there are any user login in #}
                            <h1>
                                <a href="{{ path('security_login') }}" class="btn btn-info btn-lg" role="button">
                                  Ouvrir une session 😜😜 >></a>      
                            </h1>
                        {% endif %}
                            
                    </div>
                </div>
            </div>
            {# <div class="page-header">
                <h1>Programme de Soutien à l'Ordonnateur National - FED</h1>
            </div> #}
{% endblock %}
